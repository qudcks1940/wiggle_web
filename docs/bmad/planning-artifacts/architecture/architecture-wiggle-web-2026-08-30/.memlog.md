---
scope: Wiggle Web — 이야기 아크 커리큘럼 도입에 따른 아키텍처 비준·확장
purpose: 운영 중인 브라운필드 아키텍처를 비준하고, 아크·회차·동화책 연결·접속 맥락 구분·그리미 2모드의 불변식을 고정한다
altitude: feature
updated: 2026-08-30T20:33
---

- (event) PRD(prd-wiggle-web-2026-08-30) 확정 후 아키텍처 착수. 브라운필드 — 코드 실측 스윕 진행 중. docs/security-data-model.md와 docs/architecture-mvp1.md는 은퇴한 D1/Worker/Sites/SIWC 서술이 남아 정본이 아니다.
- (direction) 산출물은 아키텍처 스파인 하나로 한정(사용자 선택). 기관 설명 자료·구현 로드맵은 이번 범위 밖 — 필요해지면 별도로 만든다. 작업 방식은 코칭.
- (constraint) PRD에서 상속되어 재논의하지 않는 것: 익명 학생 ID 소유 모델, 수업코드·QR은 입장 수단일 뿐 접근 권한 아님, 점수·순위·재능진단 스키마 금지, 그리미는 호출 시에만·평가 금지, 점선/시범은 분리 레이어로 저장 이미지 비합성, 교사 편의보다 학생 익명성 우선.
- (decision) 사용자 확정: 아크 소유 구조는 '학급은 가리키고 아이는 책을 갖는다'. 학급은 지금 어떤 아크의 몇 회차인지만 가리키는 포인터이고, 아이는 (student_id, arc_id) 단위로 자기 책을 갖는다. 교사가 아크를 바꾸면 새 책이 생기고 이전 책은 보존된다(PRD FR-2b). 반 진도는 학급 포인터가, 개인 진도는 각자 책의 빈 쪽이 담당한다.
- (decision) 사용자 확정: 완료한 회차 그림을 아이가 다시 고칠 수 있고 동화책에도 반영된다. 책 쪽은 원본 그림을 참조하고, 그림이 바뀌면 그 쪽 배치를 재계산한다.
- (constraint) [비준] DB 접근은 Drizzle 쿼리빌더가 아니라 bindings().DB.prepare/batch의 raw SQL + D1 호환 인터페이스다. db/schema.ts는 선언·generate용일 뿐 런타임 미사용. SELECT는 snake_case AS camelCase 별칭 규약.
- (constraint) [비준] 마이그레이션 러너가 없다. ensureSchema()가 프로세스당 1회 CREATE TABLE IF NOT EXISTS 배열을 batch로 실행하고 조건부 ALTER로 보강한다. 새 테이블·컬럼은 db/schema.ts와 db/runtime.ts의 schemaStatements 양쪽에 넣어야 한다.
- (constraint) [비준] 라우트 골격 고정: sameOrigin → studentFromRequest/requireTeacher → rateLimit(scope:주체id) → payload 검증 → 소유권 재조회 → requestId 멱등 → expectedRevision CAS → DB.batch(WHERE-조건 CAS) → changes 검사. 미들웨어 없이 라우트마다 SQL WHERE에 student_id/teacher_id를 직접 넣어 권한을 강제한다.
- (constraint) [비준] 동시성은 낙관적 CAS만. UPDATE ... WHERE revision=? + meta.changes 확인, batch 후속 문장은 WHERE EXISTS(last_mutation_id=requestId)로 앞 문장 성공에 종속시킨다. 락 없음.
- (constraint) [비준] 검증 규약: 순수 함수를 별도 모듈에 두고 '실패→null 반환', 통과 시 원본이 아니라 알려진 필드만 남긴 정규화 사본을 반환한다. 클라이언트와 서버가 같은 상수를 같은 모듈에서 import한다.
- (constraint) [비준] 응답은 noStoreJson/jsonError. 오류 메시지는 아이가 읽는 한국어 존댓말이고 분기가 필요하면 code를 함께 준다. R2는 공개 URL을 만들지 않고 항상 세션 검사 후 API가 프록시한다.
- (version) 실측 확인: OpenAI 모델 기본값은 process.env.OPENAI_MODEL ?? 'gpt-5.6-sol', Responses API + json_schema strict + store:false + 20초 타임아웃. react-moveable@0.56.0(MIT). Next.js App Router + Turso(libSQL) + R2(S3 API, aws4fetch).
- (event) 긴급(스파인 무관): package.json이 next 16.2.6에 고정돼 있고 CVE-2025-66478(CVSS 10.0, RSC RCE, CISA KEV 등재·실제 악용)과 AVIF 이미지 최적화 무인증 RCE에 노출된다. 둘 다 16.3.3에서만 수정. 아키텍처가 막지 않는다.
- (override) 정정: 'Vercel 함수 본문 한도가 100MB로 올랐다'는 내 앞선 진술은 문서 실측과 어긋난다. vercel.com/docs/functions/limitations(2026-08-24)는 여전히 4.5MB를 명시하고 Fluid도 올리지 않는다. 완성 PNG 분리 전송 우회는 필요하며 제거하면 저장이 깨진다.
- (change) 리뷰어 검증 반영 6건: ①패러다임 표의 'lib/*.ts 의존성 0' 과장 정정 + middleware/proxy 금지 명시 ②AD-8에 normalizeActivityKey 조용한 폴백 함정과 LESSONS[0] 의존 3곳 경고 ③AD-11에 완성 상태가 세 라우트 네 가지로 해석된다는 실측 반영(통일은 할 일이지 불변식 아님) ④AD-12의 'IP 저장 안 함'을 rate_limits.key 평문 실태로 정정 ⑤Seed에 classrooms용 조건부 ALTER 분기 부재 경고(운영만 500) ⑥낡은 문서 표에서 cf-connecting-ip 행 삭제(해당 문서에 없음), SIWC 행을 architecture-mvp1.md로 범위 축소.
- (version) 확인: Next.js 안정 최신 16.3.3(설치본 16.2.6). react-moveable 0.56.0이 npm latest이나 2023-12-03 이후 정지 상태, 알려진 취약점 0. OpenAI gpt-5.6-sol은 실재하는 현행 모델 ID(별칭 gpt-5.6, 형제 -terra/-luna).
- (event) 리뷰어 게이트 1차 판정: 통과 불가. 적대적 리뷰가 홀 11개, 루브릭 리뷰가 CRITICAL 5 + HIGH 7을 냈다. 스파인을 AD 14개에서 19개로 재작성.
- (decision) 데이터 소실 구멍 확인·차단(AD-6 확장): storybook_assets.object_key는 채택 당시 artworks.final_image_key의 스냅샷이고, PUT /api/artworks/[id] 말미가 이전 final 키를 R2에서 지운다. 지금은 완성=잠금이라 발화하지 않지만 재개봉(AD-13)이 허용되는 순간 그림책 쪽이 영구 백지가 된다. 채택된 완성 키는 저장 경로에서 삭제하지 않는다.
- (decision) 학급 포인터 위치 확정(AD-9): classrooms의 신규 컬럼 current_arc_id + current_episode_id. current_activity 키 문법을 확장하지 않는다 — normalizeActivityKey가 닫힌 허용목록 + 조용한 폴백이라 모르는 키를 오류 없이 DEFAULT_ACTIVITY_KEY로 바꿔 읽고, FR-15가 그 기본값의 근거인 LESSONS를 지운다. 스파인 자기모순(AD 본문 vs Seed) 해소.
- (decision) 아크 책은 기존 storybooks와 별개 엔티티로 확정(AD-11). 쪽의 그림 참조·이미지 키는 쪽 행의 컬럼이고 document_json에 넣지 않는다 → 회차 저장이 편집기 CAS와 충돌하지 않는다. 회차 쪽 순서는 이동 불가, 순서 이동은 수동 편집기 기능.
- (decision) 회차 귀속은 작품 생성 시점에 artworks.arc_id/episode_id로 고정(AD-10). 오프라인 큐가 며칠 뒤 flush돼도 요청 시점 학급 포인터로 쪽을 재결정하지 않는다. 회차→그림 역추적 조회 금지.
- (decision) 회차는 순서 index가 아니라 안정 episodeId로 가리키고, 회차 상수는 언제나 그 아이 책의 arcVersion으로 읽는다(AD-8). 버전 올림 누락은 체크섬 테스트로 잡는다.
- (decision) 접속 맥락의 기본값을 교실로 뒤집었다(AD-14). 판별 신호는 current_episode_id 유효성 하나이며 admission_open은 근거가 아니다. 교사가 아무것도 누르지 않아도 흐름이 막히지 않는다 — 기존 안은 FR-33(교사 대기 지점 금지)을 위반했다.
- (decision) AD-15를 정서에서 응답 형상 금지로 재작성. 빈 쪽은 artworkId: null로만 표현하고 completedCount/missingCount/progressRate 류 집계 필드를 응답 타입에 정의하지 않는다. 교사는 학급 포인터와 개별 아이의 쪽 목록만 본다 → FR-34와 FR-10b가 동시에 성립하고 타입 검사로 위반이 잡힌다.
- (decision) AD-3 확장: meta.changes 검사 의무를 조건부 INSERT까지 넓히고(경합에서 진 요청은 기존 행을 이어 쓴다), 한 batch가 두 애그리게이트를 쓰지 않는다는 규칙을 추가. 코칭 이벤트의 eventId 멱등은 명시적 예외로 비준.
- (decision) AD-13: 완성 판정을 lib의 단일 조건 함수로 만들고 세 검사 지점(작품 저장·완성 이미지 선업로드·코칭 before)이 import한다. 재개봉과 뒤따르는 저장은 서로 다른 requestId를 쓴다 — 같은 값이면 artwork_mutations 복합 PK가 두 번째를 duplicate로 삼켜 실제 저장이 조용히 버려진다.
- (question) AD-17 미해결: studentFromRequest가 classrooms.active=1을 조인 조건에 걸고 deleteClassroom·archiveStudent가 세션을 취소하므로, 그 상태에서 아이는 자기 책을 열 수 없다. FR-2b의 '보존'이 행 존속인지 열람 가능인지 정해야 한다. 둘 중 하나를 고르는 것은 제품 결정.
- (change) AD 신설: AD-7(오류·응답 의미론 비준), AD-18(계측 금지), AD-19(운영 봉투 — 환경 경계·FR-15 배포 단위·회귀 테스트 3건). 침묵했던 차원 보완.
- (change) 간결성 정리: 낡은 문서 정정 절 13줄을 '환경·문서 정본' 3줄로 축약, 다이어그램 아래 AD 재진술 산문 삭제, Deferred의 제품 미결정 4건을 PRD §11 참조로 축약. 다이어그램 3종 수정 — ARC_POINTER 엔티티 삭제, storybooks 계열 추가, 유일 제약 표기, flowchart 종단 노드 모양 정정, 시퀀스에 duplicate 분기와 책·쪽 lazy 생성 반영.
- (assumption) Deferred 사실관계 정정: 기존 '정리 작업을 아크가 새로 만들지 않는다'는 틀렸다. 아크는 아이×아크마다 책 1 + 쪽 N + R2 객체 N을 새로 만들고 FR-2b가 영구 보존을 요구하므로 누적량이 아크 전환마다 선형으로 는다.
- (change) binds 목록을 실제 구속하는 FR만 남기도록 정정했다. FR-6·FR-9·FR-23·FR-25는 AD가 구속하지 않으므로 제거(각각 일반 규칙·UX 소관·수동 편집기 기존 기능).
- (decision) 사용자 확정(2026-08-30): 학급을 삭제하면 그 학급 아이는 자기 그림·동화책을 더 이상 열 수 없다. AD-17을 열린 질문에서 [ADOPTED] 비준으로 전환 — 현행 동작(studentFromRequest의 active=1 조인, deleteClassroom의 세션 일괄 취소)이 의도된 것이다. FR-2b의 '보존'은 아크 전환에만 적용된다.
- (decision) restoreClassroom이 존재하지 않으므로 학급 삭제는 되돌릴 수 없다. 따라서 교사 확인 문구가 '내부 데이터는 복구를 위해 안전하게 보관됩니다'라고 말하는 것은 거짓이며 교체한다. archiveStudent는 restoreStudent가 있어 기존 문구가 참이므로 그대로 둔다.
- (decision) 사용자 확정(2026-08-30): 책·쪽 테이블, 그림책 편집기(FR-23/25), 책 넘겨보기(FR-24)는 모두 다른 팀 소유다. 아크 작업은 책을 만들지 않는다.
- (decision) AD-10을 팀 간 계약으로 재작성: artworks.arc_id + episode_id가 회차 귀속의 유일한 정본이고 아크 팀이 쓰고 책 팀은 읽기만 한다. 기존 '쪽 행이 유일하게 결정한다'는 책이 다른 팀 소유가 되면서 방향이 뒤집혔다.
- (decision) AD-10에 유일성 제약 신설: (student_id, arc_id, episode_id)에 완성 작품은 최대 하나. 없으면 책 팀이 어느 그림을 쪽에 걸지 스스로 판단해야 하고 두 팀의 답이 갈린다.
- (decision) AD-11을 소유 경계로 재작성. 아크 팀은 책·쪽 테이블을 만들지도 읽지도 않고 storybooks도 건드리지 않는다. 회차 순서는 아크 상수가 정본이며 책 팀이 재정의하지 않는다. 빈 쪽 표현은 책 팀 소관이되 AD-15 응답 형상 금지는 팀이 달라도 적용된다.
- (assumption) 팀 경계의 가장 취약한 지점은 AD-6의 완성 이미지 키 삭제 금지다. 아크 팀이 저장 경로에서 키를 지우면 책 팀의 쪽이 백지가 되고, 두 팀이 각자 테스트하면 잡히지 않는다.
